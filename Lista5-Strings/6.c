#include <stdio.h>

int main() {
    char s[] = "ola 2mundo";

    for (int i = 0; s[i] != '\0'; i++) {
        if (s[i] >= 48 && s[i] <= 57) {
            
        }
    }

    printf("%s\n", s);
    return 0;
}
